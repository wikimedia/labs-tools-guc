{
	"require-dev": {
		"squizlabs/php_codesniffer": "1.*"
	},
	"scripts": {
		"test": "phpcs . --standard=phpcs.xml --ignore=vendor/* --extensions=php"
	},
	"require": {
		"Krinkle/toollabs-base": "^0.8.0"
	}
}
